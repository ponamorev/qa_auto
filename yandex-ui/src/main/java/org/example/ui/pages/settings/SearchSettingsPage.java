package org.example.ui.pages.settings;

import org.openqa.selenium.WebDriver;

public class SearchSettingsPage extends SettingsBasePage {
    public SearchSettingsPage(WebDriver driver) {
        super(driver);
    }
}
